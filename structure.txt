Renderer
    Canvas (pixel array)
    Camera
    mat_proj
    vec<triangles>

Canvas
    Pixel array (u8 array)
    (Z buffer array)
    clear(), put_pixel(), viewport_to_canvas()

Mesh
    vector<Vec3> vertices;
    indices ?

Model
    position
    scale
    rotation
    queue for drawing (do all the nececary calculations, and clipping), then add the triangles to Renderer.vec<triangles>

--------------------

Triangle
    Vertex
        pos Vec3
        color u32
        tex coord?
        normal [Vec3; 3]
    ...
